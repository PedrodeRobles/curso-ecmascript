let hello = 'Hello';
let world = 'World';
let epicPhrase = hello + ' ' + world + '!';
console.log(epicPhrase);

// Template literals
let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);

// Multi-line strings
let lorem = 'Esto es un string normal \n' + 'Esto es otra línea';
console.log(lorem);

let lorem2 = `Esta es una frase epica
Aca continua la frase epica
`;
console.log(lorem2);